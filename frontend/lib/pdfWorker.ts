// // lib/pdfWorker.ts
// import { GlobalWorkerOptions } from "pdfjs-dist/legacy/build/pdf";

// if (typeof window !== "undefined" && "Worker" in window) {
//   // need to use legacy build since newest don't have workers yet
//   GlobalWorkerOptions.workerSrc = `//unpkg.com/pdfjs-dist@${GlobalWorkerOptions.workerVersion}/legacy/build/pdf.worker.min.js`;
// }
